# ==========================================
# 🔧 ENVIRONMENT CONFIGURATION
# ==========================================
# This file contains all environment variables used by the application.
# Copy this file to .env.local and update with your actual values.
# 
# 🚨 IMPORTANT: Never commit .env.local or any file containing real credentials!
# ==========================================

# ==========================================
# 🗄️ SUPABASE CONFIGURATION
# ==========================================
# These variables are required for the application to connect to Supabase.
# You can find these values in your Supabase project settings.

# Supabase Project URL - Found in Settings > API
# Used for: Client-side database connections and authentication
VITE_SUPABASE_URL=your_supabase_project_url

# Supabase Anonymous Key - Found in Settings > API
# Used for: Client-side authentication and public API access
# This key is safe to use in frontend code
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key

# Supabase Service Role Key - Found in Settings > API
# Used for: Server-side operations, edge functions, and admin tasks
# ⚠️ WARNING: This key bypasses Row Level Security - keep it secret!
SUPABASE_SERVICE_ROLE_KEY=your_supabase_service_role_key

# Supabase User ID (Optional)
# Used for: Seeding scripts and development testing
# This is the UUID of a test user in your Supabase auth.users table
SUPABASE_USER_ID=your_test_user_uuid

# ==========================================
# 📧 EMAIL CONFIGURATION
# ==========================================
# Email service configuration for sending transactional emails

# Resend API Key - Get from https://resend.com/api-keys
# Used for: Sending invitation emails, notifications, and contact form submissions
# Required for: send-invitation function, contact form functionality
RESEND_API_KEY=your_resend_api_key

# ==========================================
# 🌐 APPLICATION CONFIGURATION
# ==========================================
# General application settings

# Application URL
# Used for: Generating absolute URLs for emails, redirects, and sharing links
# Format: https://your-domain.com (no trailing slash)
# Examples: 
#   - Development: http://localhost:5173
#   - Production: https://your-app-domain.com
APP_URL=http://localhost:5173

# ==========================================
# 📝 NOTES FOR DEVELOPERS
# ==========================================
# 1. Client-side variables must be prefixed with VITE_ to be accessible in the browser
# 2. Server-side variables (like SUPABASE_SERVICE_ROLE_KEY) should NOT have VITE_ prefix
# 3. Edge functions in Supabase have automatic access to SUPABASE_URL and SUPABASE_SERVICE_ROLE_KEY
# 4. For local development, create a .env.local file (git-ignored) with your actual values
# 5. For production deployment, set these variables in your hosting platform's environment settings
# ==========================================